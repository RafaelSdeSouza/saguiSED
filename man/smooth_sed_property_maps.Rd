% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smooth_property_maps.R
\name{smooth_sed_property_maps}
\alias{smooth_sed_property_maps}
\title{Smooth SED-fit property maps on the SAGUI region graph}
\usage{
smooth_sed_property_maps(
  labels,
  fit,
  properties = c("logMformed", "SFR", "logSFR", "logsSFRformed", "age_gyr", "logZ_Zsun",
    "Av", "fit_rms_sigma"),
  lambda = 3,
  adjacency = c("queen", "rook"),
  unsmoothed_properties = "fit_rms_sigma",
  keep_na_outside = TRUE
)
}
\arguments{
\item{labels}{Region-label matrix or a SAGUI segmentation object containing
\code{cluster_map}.}

\item{fit}{A \code{sagui_sed_fit} object returned by \code{\link[=fit_region_seds]{fit_region_seds()}}.}

\item{properties}{Fit-summary columns to smooth and paint.}

\item{lambda}{Smoothing strength passed to
\code{sagui::smooth_region_field_laplacian()}.}

\item{adjacency}{Neighborhood definition, either \code{"queen"} or \code{"rook"}.}

\item{unsmoothed_properties}{Properties to paint directly without smoothing.
Diagnostics such as \code{fit_rms_sigma} should usually remain unsmoothed.}

\item{keep_na_outside}{Keep non-region pixels as \code{NA}.}
}
\value{
A named list of matrices.
}
\description{
This is a post-processing step. It does not refit the photometry and does not
alter the independent SED-fit summaries returned by \code{\link[=fit_region_seds]{fit_region_seds()}}.
Instead, it regularizes region-level physical quantities over the adjacency
graph of the segmentation using \code{sagui::smooth_region_field_laplacian()}.
}
